[project]
name = "mock-api"
version = "0.1.0"
description = "Mock API — AI-агент проверки льготных кредитов"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.138.1",
    "python-multipart>=0.0.32",
    "uvicorn>=0.49.0",
]
